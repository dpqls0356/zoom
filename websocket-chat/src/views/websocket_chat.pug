doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title RandomChat
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body
        header
            h1 RandomChat
        main
            div#saveName
                form#saveNameForm
                    div 채팅에 사용할 닉네임을 지정하세요
                    input(type="text", placeholer="write a name", required)
                    button#nameSaveBt 저장
            div#chat(style="display: none;")
                div 
                    ul#messageList
                form#mesaageForm
                    input(type="text", placeholder="write a message", required)
                    button#messageSendBtn 보내기
        script(src="/public/js/websocket_chat.js") 